version: '3.8'

services:
  web:
    image: ${REGISTRY_USER}/uiverse-hub:latest
    container_name: uiverse-hub-app
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
      - APP_URL=${APP_URL}
    restart: always
